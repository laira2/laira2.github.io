---
title: "[프로그래머스] 완주하지 못한 선수 - Java | 해시"
date: 2025-05-11
categories: [코딩테스트]
tags: [프로그래머스, Java, 해시]
layout: post
---

### 🧩 문제 요약

- 마라톤 참가자 명단과 완주자 명단이 주어짐
- 단 한 명의 선수가 완주하지 못함
- 그 선수를 찾아 반환

---

### 💡 풀이 전략

- 참가자 명단을 해시맵에 저장 → 이름별 등장 횟수 세기
- 완주자 명단을 돌면서 등장 횟수 차감
- 마지막에 **값이 1 남은 이름이 완주 못한 선수**

---

### ✅ Java 코드

```java
import java.util.*;

class Solution {
    public String solution(String[] participant, String[] completion) {
        Map<String, Integer> map = new HashMap<>();
        
        for (String p : participant) {
            map.put(p, map.getOrDefault(p, 0) + 1);
        }
        
        for (String c : completion) {
            map.put(c, map.get(c) - 1);
        }
        
        for (String key : map.keySet()) {
            if (map.get(key) != 0) return key;
        }
        
        return "";
    }
}
```

---

### 🧠 시간복잡도
참가자 수: N

해시맵 삽입 & 탐색 → 평균 O(1)

총 시간복잡도: O(N)

---

### 🔍 트러블슈팅
처음엔 Arrays.sort()로 푸는 방법도 생각했지만,
해시맵 방식이 더 빠르고 깔끔했음


